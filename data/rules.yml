version: "3.1"

rules:
  - rule: show fipu
    steps:
      - intent: search_poduzeca_praksa
      - action: action_show_data

  - rule: get detail
    steps:
      - intent: detail
      - action: action_get_detail
